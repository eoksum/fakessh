# Fake SSH
Fake SSH made with Paramiko (Python 3) to detect brute forcers in the wild.
Logs each login attempt in format *Date,Source IP,Source Port,Login Attempts,Login Username,Login Password* to ssh_bruteforce_logs.txt file it generates locally.
